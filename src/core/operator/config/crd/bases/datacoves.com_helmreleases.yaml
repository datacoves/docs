---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: helmreleases.datacoves.com
spec:
  group: datacoves.com
  names:
    kind: HelmRelease
    listKind: HelmReleaseList
    plural: helmreleases
    singular: helmrelease
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HelmRelease is the Schema for the helmreleases API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HelmReleaseSpec defines the desired state of HelmRelease
            properties:
              chart:
                description: The chart to install (e.g. apache-airflow/airflow)
                type: string
              repoName:
                description: A name for the helm repo (e.g. apache-airflow)
                type: string
              repoURL:
                description: The helm repo url (e.g. https://airflow.apache.org)
                type: string
              valuesName:
                description: The name of a secret containing the values.yaml to configure
                  the chart.
                type: string
              version:
                description: The version of the chart to install (e.g. 1.3.0)
                type: string
            required:
            - chart
            - repoName
            - repoURL
            - valuesName
            - version
            type: object
          status:
            description: HelmReleaseStatus defines the observed state of HelmRelease
            properties:
              status:
                description: Status is the status of a release
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
